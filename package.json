{
  "name": "shree350",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postbuild": "node -e \"const fs=require('fs');const a=fs.readdirSync('dist/assets');if(!a.some(f=>f.endsWith('.js'))){console.error('Build missing JS bundle');process.exit(1)}console.log('Build OK:',a.join(', '))\" "
  },
  "dependencies": {
    "framer-motion": "^12.42.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-icons": "^5.7.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-react": "^6.0.3",
    "tailwindcss": "^4.3.3",
    "vite": "^8.1.1"
  }
}
